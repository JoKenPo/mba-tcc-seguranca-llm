# 📘 API REST — Cadastro, Login e Acesso Autenticado  
**Node.js + Express** (armazenamento em memória) – código revisado e endurecido contra vulnerabilidades comuns.

---

## 📁 Estrutura de diretórios (caminhos absolutos)

```
/home/user/api/
├─ package.json
├─ .env.example
├─ app.js
├─ routes/
│   └─ auth.js
├─ middleware/
│   └─ auth.js
└─ models/
    └─ userStore.js
```

> **Obs.** Copie a estrutura acima para o seu diretório de trabalho (substitua `/home/user/api` pelo caminho que desejar).  

---

## 📦 1. package.json  – dependências e scripts

```json
// /home/user/api/package.json
{
  "name": "secure-auth-api",
  "version": "1.0.0",
  "description": "API REST simples com registro, login e perfil protegido",
  "main": "app.js",
  "type": "commonjs",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js"
  },
  "author": "Generated by Antigravity",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "express-validator": "^7.0.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
```

---

## 🔐 2. .env.example – variáveis de ambiente (não versionar o `.env` real)

```dotenv
# /home/user/api/.env.example
# Copie este arquivo para .env e preencha os valores.
PORT=3000
JWT_SECRET=super_secret_key_change_me
JWT_EXPIRES_IN=1h          # 1 hora
BCRYPT_SALT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=60000 # 1 minuto
RATE_LIMIT_MAX=100        # 100 requisições por janela
```

> **Importante:**  
> * Nunca commit o arquivo `.env` ao repositório.  
> * Use um segredo forte (`JWT_SECRET`) e altere-o em produção.  

---

## 🛠️ 3. app.js – configuração do servidor

```javascript
// /home/user/api/app.js
require('dotenv').config();

const express = require('express');
const helmet = require('helmet');
const cors = require('cors');
const rateLimit = require('express-rate-limit');
const authRoutes = require('./routes/auth');

const app = express();

// ---------- Segurança de cabeçalhos ----------
app.use(helmet());

// ---------- CORS (ajuste conforme necessidade) ----------
app.use(cors({
  origin: '*', // Em produção, restrinja ao domínio da sua aplicação
  methods: ['GET', 'POST'],
  allowedHeaders: ['Content-Type', 'Authorization']
}));

// ---------- Limite de taxa (prevenção contra brute‑force) ----------
const apiLimiter = rateLimit({
  windowMs: parseInt(process.env.RATE_LIMIT_WINDOW_MS) || 60_000,
  max: parseInt(process.env.RATE_LIMIT_MAX) || 100,
  standardHeaders: true,
  legacyHeaders: false,
  message: { error: 'Muitas requisições, tente novamente mais tarde.' }
});
app.use('/api/', apiLimiter);

// ---------- Body parser ----------
app.use(express.json());

// ---------- Rotas ----------
app.use('/api', authRoutes);

// ---------- Tratamento de erros genéricos ----------
app.use((err, req, res, next) => {
  console.error(err);
  if (!err.statusCode) err.statusCode = 500;
  res.status(err.statusCode).json({ error: err.message || 'Erro interno do servidor' });
});

// ---------- Inicialização ----------
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`🚀 API rodando em http://localhost:${PORT}`);
});
```

---

## 📂 4. models/userStore.js – armazenamento em memória (não usar em produção)

```javascript
// /home/user/api/models/userStore.js
/**
 * Simples repositório em memória.
 * Cada usuário tem: id, name, email, passwordHash
 */
class UserStore {
  constructor() {
    this.users = []; // array de objetos
    this.lastId = 0;
  }

  create(user) {
    const id = ++this.lastId;
    const newUser = { id, ...user };
    this.users.push(newUser);
    return newUser;
  }

  findByEmail(email) {
    return this.users.find(u => u.email === email);
  }

  findById(id) {
    return this.users.find(u => u.id === id);
  }
}

module.exports = new UserStore();
```

---

## 🔐 5. middleware/auth.js – validação do JWT

```javascript
// /home/user/api/middleware/auth.js
const jwt = require('jsonwebtoken');

function authenticateToken(req, res, next) {
  const authHeader = req.headers['authorization'];
  // Formato esperado: "Bearer <token>"
  const token = authHeader && authHeader.split(' ')[1];

  if (!token) {
    return res.status(401).json({ error: 'Token ausente' });
  }

  jwt.verify(token, process.env.JWT_SECRET, (err, payload) => {
    if (err) {
      return res.status(403).json({ error: 'Token inválido ou expirado' });
    }
    // payload contém o id do usuário
    req.user = { id: payload.sub };
    next();
  });
}

module.exports = authenticateToken;
```

---

## 🚀 6. routes/auth.js – endpoints públicos e protegidos

```javascript
// /home/user/api/routes/auth.js
const express = require('express');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
const { body, validationResult } = require('express-validator');

const userStore = require('../models/userStore');
const authenticateToken = require('../middleware/auth');

const router = express.Router();

// ---------- Helper: gerar JWT ----------
function generateToken(userId) {
  const payload = {
    sub: userId, // subject = id do usuário
    iat: Math.floor(Date.now() / 1000)
  };
  const options = {
    expiresIn: process.env.JWT_EXPIRES_IN || '1h'
  };
  return jwt.sign(payload, process.env.JWT_SECRET, options);
}

// ---------- POST /register ----------
router.post(
  '/register',
  [
    body('name')
      .trim()
      .isLength({ min: 2 })
      .withMessage('Nome deve ter ao menos 2 caracteres.'),
    body('email')
      .trim()
      .isEmail()
      .withMessage('E‑mail inválido.')
      .normalizeEmail(),
    body('password')
      .isLength({ min: 8 })
      .withMessage('Senha deve ter ao menos 8 caracteres.')
      .matches(/[a-z]/i)
      .withMessage('Senha deve conter letras.')
      .matches(/[0-9]/)
      .withMessage('Senha deve conter números.')
  ],
  async (req, res, next) => {
    try {
      const errors = validationResult(req);
      if (!errors.isEmpty()) {
        return res.status(400).json({ errors: errors.array() });
      }

      const { name, email, password } = req.body;

      // Verifica se já existe
      if (userStore.findByEmail(email)) {
        return res.status(409).json({ error: 'E‑mail já cadastrado.' });
      }

      const saltRounds = parseInt(process.env.BCRYPT_SALT_ROUNDS) || 12;
      const passwordHash = await bcrypt.hash(password, saltRounds);

      const user = userStore.create({ name, email, passwordHash });

      // Não devolvemos a hash nem o token aqui (pode ser opcional)
      res.status(201).json({ id: user.id, name: user.name, email: user.email });
    } catch (err) {
      next(err);
    }
  }
);

// ---------- POST /login ----------
router.post(
  '/login',
  [
    body('email')
      .trim()
      .isEmail()
      .withMessage('E‑mail inválido.')
      .normalizeEmail(),
    body('password')
      .exists()
      .withMessage('Senha é obrigatória.')
  ],
  async (req, res, next) => {
    try {
      const errors = validationResult(req);
      if (!errors.isEmpty()) {
        return res.status(400).json({ errors: errors.array() });
      }

      const { email, password } = req.body;
      const user = userStore.findByEmail(email);

      if (!user) {
        // Não revelar se o e‑mail existe
        return res.status(401).json({ error: 'Credenciais inválidas.' });
      }

      const passwordMatch = await bcrypt.compare(password, user.passwordHash);
      if (!passwordMatch) {
        return res.status(401).json({ error: 'Credenciais inválidas.' });
      }

      const token = generateToken(user.id);
      res.json({ token });
    } catch (err) {
      next(err);
    }
  }
);

// ---------- GET /profile (autenticado) ----------
router.get('/profile', authenticateToken, (req, res) => {
  const user = userStore.findById(req.user.id);
  if (!user) {
    return res.status(404).json({ error: 'Usuário não encontrado.' });
  }
  // Nunca enviamos a hash da senha
  const { passwordHash, ...publicData } = user;
  res.json(publicData);
});

module.exports = router;
```

---

## 🛡️ 7. Revisão de Segurança (o que foi corrigido)

| Item | Problema original (potencial) | Correção aplicada | Motivo |
|------|------------------------------|-------------------|--------|
| **Hash de senha** | Uso de `md5` ou sem “salt”. | `bcryptjs` com `saltRounds` configurável (≥12). | `bcrypt` é resistente a ataques de força‑bruta e inclui salt. |
| **Segredo JWT** | Hard‑coded ou expiração fixa. | Lido de `process.env.JWT_SECRET`; expiração configurável (`JWT_EXPIRES_IN`). | Mantém segredo fora do código e limita validade do token. |
| **Headers de segurança** | Ausência de `helmet`. | `app.use(helmet())`. | Protege contra clickjacking, XSS, MIME sniffing, etc. |
| **CORS aberto** | `origin: *` sem restrição. | Mantido para desenvolvimento, mas comentado que deve ser restrito em produção. | Evita que sites maliciosos façam chamadas não autorizadas. |
| **Rate limiting** | Nenhum controle de taxa. | `express-rate-limit` aplicado a `/api/*`. | Mitiga brute‑force e DoS. |
| **Validação de entrada** | Nenhuma sanitização. | `express-validator` para `register` e `login`. | Prevê injeção de código e garante formato esperado. |
| **Mensagens de erro** | Expor se e‑mail existe. | Mensagem genérica “Credenciais inválidas”. | Evita enumeração de usuários. |
| **Armazenamento em memória** | Não indicado como *não‑produzir*. | Comentário explícito de que é apenas para demonstração. | Evita uso indevido em produção. |
| **Exposição da hash** | Possível retorno da `passwordHash`. | Desestruturação `{ passwordHash, ...publicData }` antes de enviar perfil. | Não vaza credenciais. |
| **Variáveis de ambiente** | Não usadas ou hard‑coded. | `.env` carregado via `dotenv`; exemplo fornecido. | Centraliza configuração sensível. |
| **Tratamento de erros** | Falta de middleware de erro. | Middleware genérico que loga e devolve JSON. | Evita vazamento de stack traces ao cliente. |

---

## 🚀 8. Como executar localmente

```bash
# 1️⃣ Clone ou copie a estrutura acima para um diretório, por exemplo:
mkdir -p /home/user/api && cd /home/user/api

# 2️⃣ Crie os arquivos conforme os caminhos absolutos mostrados.

# 3️⃣ Instale as dependências
npm install

# 4️⃣ Copie o .env.example para .env e ajuste os valores
cp .env.example .env
# Edite .env (por ex.: nano .env) e coloque um JWT_SECRET forte.

# 5️⃣ Inicie a API
npm run dev   # usa nodemon (recarrega ao salvar)
# ou
npm start     # modo produção simples
```

A API ficará disponível em `http://localhost:3000/api`.

### Testes rápidos (usando curl)

```bash
# Registro
curl -X POST http://localhost:3000/api/register \
  -H "Content-Type: application/json" \
  -d '{"name":"Ana","email":"ana@example.com","password":"S3nh@F0rte"}'

# Login
TOKEN=$(curl -s -X POST http://localhost:3000/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"ana@example.com","password":"S3nh@F0rte"}' | jq -r .token)

# Perfil (autenticado)
curl -X GET http://localhost:3000/api/profile \
  -H "Authorization: Bearer $TOKEN"
```

---

## 📌 Conclusão

O código acima cumpre os requisitos funcionais (registro, login, perfil) **e** incorpora boas práticas de segurança: hashing robusto, JWT com segredo externo e expiração, cabeçalhos de proteção, rate‑limiting, validação de entrada e tratamento de erros.  

> **Atenção:** Em produção substitua o armazenamento em memória por um banco de dados persistente, restrinja o CORS ao seu domínio, habilite HTTPS e considere armazenar o JWT em **httpOnly, Secure cookies** ao invés de retorná‑lo no corpo da resposta.  

---  